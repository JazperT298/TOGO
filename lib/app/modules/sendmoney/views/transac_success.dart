import 'package:flutter/material.dart';

class TransacSuccessView extends StatelessWidget {
  const TransacSuccessView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
